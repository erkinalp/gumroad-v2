<div class="crowdsourcing-hub">
  <div class="hub-header">
    <div class="header-content">
      <h1>Crowdsourcing & Automation</h1>
      <p class="subtitle">Manage surveys, tasks, and automated personalized messages</p>
    </div>
    <div class="header-actions">
      <%= link_to "API Documentation", "/docs/api_v2_surveys_messages.md", class: "btn btn-outline" %>
    </div>
  </div>

  <div class="stats-overview">
    <div class="stat-box">
      <span class="stat-number"><%= @active_surveys %></span>
      <span class="stat-label">Active Surveys</span>
    </div>
    <div class="stat-box">
      <span class="stat-number"><%= @total_responses %></span>
      <span class="stat-label">Total Responses</span>
    </div>
    <div class="stat-box">
      <span class="stat-number"><%= @messages_sent %></span>
      <span class="stat-label">Messages Sent</span>
    </div>
    <div class="stat-box">
      <span class="stat-number"><%= @active_templates %></span>
      <span class="stat-label">Active Templates</span>
    </div>
  </div>

  <div class="tools-grid">
    <!-- Surveys Card -->
    <div class="tool-card survey-card">
      <div class="card-icon">📋</div>
      <h2>Surveys & Tasks</h2>
      <p>Create and manage surveys for data collection, feedback, and crowdsourcing tasks.</p>

      <div class="card-actions">
        <%= link_to "Manage Surveys", surveys_path, class: "btn btn-primary" %>
      </div>

      <div class="quick-links">
        <% if @surveys_count == 0 %>
          <div class="empty-hint">No surveys yet. Create one from your products page.</div>
        <% else %>
          <%= link_to "View Results", surveys_path, class: "link" %>
        <% end %>
      </div>
    </div>

    <!-- Automated Messages Card -->
    <div class="tool-card message-card">
      <div class="card-icon">💬</div>
      <h2>Automated Messages</h2>
      <p>Set up personalized, triggered messages (OnlyFans style) with A/B testing.</p>

      <div class="card-actions">
        <%= link_to "Manage Templates", message_templates_path, class: "btn btn-primary" %>
        <%= link_to "View Inbox", automated_messages_path, class: "btn btn-secondary" %>
      </div>

      <div class="quick-links">
        <div><%= @templates_count %> templates created</div>
      </div>
    </div>

    <!-- API Integration Card -->
    <div class="tool-card api-card">
      <div class="card-icon">🔌</div>
      <h2>API & Automation</h2>
      <p>Connect external verifiers, sanitizers, and quality control tools.</p>

      <div class="code-preview">
        <code>POST /api/v2/surveys/:id/responses</code>
      </div>

      <div class="card-actions">
        <button onclick="copyApiToken()" class="btn btn-secondary">Copy API Token</button>
      </div>

      <div class="quick-links">
        <a href="/api-docs" class="link">View Full API Reference</a>
      </div>
    </div>
  </div>

  <div class="recent-activity">
    <h3>Quick Actions</h3>
    <div class="action-buttons">
      <%= link_to "Go to Products", "/links", class: "btn btn-outline" %>
      <%# Placeholder for hypothetical routes %>
      <%= link_to "Go to Posts", "/posts", class: "btn btn-outline" %>
    </div>
  </div>
</div>

<style>
.crowdsourcing-hub {
  max-width: 1200px;
  margin: 40px auto;
  padding: 20px;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

.hub-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 40px;
}

.hub-header h1 {
  font-size: 32px;
  margin-bottom: 8px;
  color: #1a1a1a;
}

.subtitle {
  color: #666;
  font-size: 18px;
}

.stats-overview {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
  margin-bottom: 40px;
}

.stat-box {
  background: white;
  padding: 24px;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.05);
  text-align: center;
  border: 1px solid #eee;
}

.stat-number {
  display: block;
  font-size: 36px;
  font-weight: 700;
  color: #007bff;
  margin-bottom: 4px;
}

.stat-label {
  color: #666;
  font-size: 14px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.tools-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 30px;
  margin-bottom: 40px;
}

.tool-card {
  background: white;
  border-radius: 16px;
  padding: 32px;
  border: 1px solid #eaeaea;
  transition: transform 0.2s, box-shadow 0.2s;
  display: flex;
  flex-direction: column;
}

.tool-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 24px rgba(0,0,0,0.08);
  border-color: #007bff;
}

.card-icon {
  font-size: 40px;
  margin-bottom: 20px;
}

.tool-card h2 {
  font-size: 24px;
  margin-bottom: 12px;
  color: #1a1a1a;
}

.tool-card p {
  color: #666;
  line-height: 1.5;
  margin-bottom: 24px;
  flex-grow: 1;
}

.card-actions {
  display: flex;
  gap: 12px;
  margin-bottom: 20px;
  flex-wrap: wrap;
}

.btn {
  padding: 12px 20px;
  border-radius: 8px;
  font-weight: 600;
  text-decoration: none;
  cursor: pointer;
  border: none;
  font-size: 14px;
  transition: background 0.2s;
}

.btn-primary {
  background: #007bff;
  color: white;
}

.btn-primary:hover {
  background: #0056b3;
}

.btn-secondary {
  background: #e9ecef;
  color: #333;
}

.btn-secondary:hover {
  background: #dde2e6;
}

.btn-outline {
  background: transparent;
  border: 1px solid #dee2e6;
  color: #666;
}

.btn-outline:hover {
  border-color: #007bff;
  color: #007bff;
}

.quick-links {
  padding-top: 16px;
  border-top: 1px solid #eee;
  font-size: 14px;
  color: #666;
}

.link {
  color: #007bff;
  text-decoration: none;
}

.link:hover {
  text-decoration: underline;
}

.code-preview {
  background: #2d2d2d;
  color: #eee;
  padding: 12px;
  border-radius: 6px;
  font-family: monospace;
  font-size: 12px;
  margin-bottom: 20px;
}

@media (max-width: 768px) {
  .stats-overview {
    grid-template-columns: 1fr 1fr;
  }
}
</style>

<script>
function copyApiToken() {
  // Mock functionality for demo
  alert("Copy your API token from your User Settings page to simplify security.");
}
</script>
